---
- name: Django | Git | Pull sources from the repository.
  sudo_user: ${project.user}
  git: >
    repo=${repo.url}
    dest=${project.root}code/
    version=${repo.branch}
    force=yes
    update=yes
  tags:
    - repo
    - application

